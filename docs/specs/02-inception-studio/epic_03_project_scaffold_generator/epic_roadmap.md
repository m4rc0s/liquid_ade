# Roadmap: Project Scaffold Generator & New Workspace Flow

- **Now:** Endpoint `/api/workspace/new` emitting canonical SCPE files.
- **Next:** Modal in React UI with form inputs and directory picker.
- **Later:** Git initialization (`git init`) option during project setup.
